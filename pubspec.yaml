name: gold_trading_simulator
description: A simulated gold trading app with live charting and backtesting.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  webview_flutter: ^4.4.2
  collection: ^1.18.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets:
    - assets/chart/chart.html
