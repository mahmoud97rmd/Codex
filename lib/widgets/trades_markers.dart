import 'package:flutter/material.dart';

class TradesMarkers extends StatelessWidget {
  const TradesMarkers({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
